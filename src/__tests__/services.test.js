import {setStorage, getStorage} from '../services';

const expect = require('chai').expect;

describe('services', () => {

  //TODO unable to unit test localStorage

  // it('setStorage({key, value}) && getStorage(key)', () => {
  //   const data = {key: 'test1', value: 'test2'};
  //   setStorage(data);
  //   expect(getStorage(data.key)).to.have.string(data.value);
  // });
});